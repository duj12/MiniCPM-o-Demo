"""离线分析工具（**不在实时路径上**）。

与 ``orchestrator/tests/`` 的区别：``tests/`` 是验证脚本（跑一遍给结论），
这里是可被验证脚本与运维流程复用的算法组件。

之所以单独成包：``delay_estimate.AcousticDelayTracker`` 原本住在
``audio/ref_track.py`` 里，被 ``session._maybe_update_delay`` 在**实时路径**
上调用。但实测证实该自适应估计在真机（iPhone）上收敛不了 —— 一旦被单个
噪声峰钉死在错值上就会**永久失效**（详见该文件 458-469 行的事故记录）。
现在 D 改为**每台设备离线测一次**的固定常量，实时路径不再需要这个估计器，
于是把它挪到这里：运行时路径干净，离线能力（和 4 个测试文件的覆盖）保留。
"""
